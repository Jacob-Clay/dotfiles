---
- name: Machine setup
  hosts: localhost
  become: true
  connection: local
  gather_facts: true
  vars:
    remote_regular_user: "{{ ansible_facts['env']['SUDO_USER'] or default(ansible_facts['user_id']) }}"
  roles:
    - system
    - neovim
    - shell
    - services
